# Archive

The V3 main tree intentionally contains only the frozen core runtime path. Historical planner variants and unrelated competition packages are not duplicated here; they remain recoverable from Git history and prior source archives.

`experimental/` contains selected non-canonical transport experiments retained only for engineering traceability. They are **not** the documented V3 runtime path.
